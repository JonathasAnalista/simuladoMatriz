# Backend Express (.env example)
# Copie este arquivo para `.env` e preencha com os valores reais.

# URL pública do frontend (usada para redirecionamentos pós-pagamento e CORS)
FRONTEND_URL=https://www.seu-dominio.com.br

# URL pública do backend (precisa ser https para receber webhooks do Mercado Pago)
BASE_URL=https://seu-backend-publico.example.com

# Porta local do servidor
PORT=8080

# Credenciais Mercado Pago
MP_ACCESS_TOKEN=seu_token_do_mercado_pago

# Firebase Admin SDK (credenciais do serviço)
FIREBASE_PROJECT_ID=seu-projeto-firebase
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xyz@seu-projeto-firebase.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# (Opcional) Origens adicionais liberadas no CORS, separadas por vírgula
CORS_ORIGINS=
# Multi-cliente (opcional): informe um JSON com os tenants do backend
# TENANTS_CONFIG_PATH=backend/config/tenants.config.json
# ou defina TENANTS_CONFIG_JSON com o conteúdo do arquivo em formato string.
